####Dev Stack Builder Website.

####This is a user friendly functional website where the user can interact with the website. With a navbar, hero section banner section, footer and an intersting stack section where there are 10 cards which can be slected and when selected a toast will show and a number and the item will be added to the stack card.

####Technologies used are:
React
Tailwind
React icons
React Toastify
Vite
Typescript

####Features:
1.A functional stack section with cards.
2.Toast notifications
3.prevents duplication
4.has remove functionality
5.can add card to the stack and can remove
6.partially responsive.
7.Have gradient brand theme.


####Also answer these React questions at the end of your Readme (write the answers in your own words, short and simple):

What is JSX, and why is it used in React?
-Used in raect ,it makes code easy to give structure to the website like html.

What is the difference between props and state?
-Props are the properties informations given to the child from the parent .used to make components dynamic. State is used to change a information or data. to make up to date.  

What does the useState hook do, and where did you use it in this project?
-useState remember the selected items and update thr UI whwnever the selection changes. i have used it for my stack section in the project.

What does the useEffect hook do, and why did you need it to load the JSON data?
Why does every item in a .map() list need a unique key prop?
-needs an unique key to identify the eaxct one which needs to add or renove or to do operation. 

What is conditional rendering? Show one place you used it (example: the empty stack message).
-If a condition is true or not and based on this what have to be the action. we use conditional rendering. I have used it in case of remove all button , Stack card whhen i have to add items and not. 

How do you pass data from a parent component to a child component, and how does a child send something back to the parent?
-by Props i can pass data from parent to child. child uses parents function and calls it when needed.




